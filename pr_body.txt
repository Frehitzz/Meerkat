## Type
Feature

## Summary
Completes Phase 3 and Phase 4 of the Meta App Integration by implementing Facebook Login for Business (OAuth flow) and incoming webhook ingestion. Includes UI updates and backend route handling. Additionally updates models.py to use enums for safer type checking, and adds an AI automation safety rule to AGENTS.md.

## Changes
- Created `meerkat-backend/routes/auth.py` for OAuth flow endpoints.
- Created `meerkat-backend/routes/webhooks.py` for verification and webhook ingestion.
- Registered new routers in `meerkat-backend/main.py`.
- Updated `meerkat-backend/models.py` to add `Message` model and use `Platform` Enum for platform typing.
- Added "Login with Facebook" button to React frontend in `meerkat-frontend/src/App.jsx`.
- Added new rule to `AGENTS.md` preventing automatic git commits/PRs by AI agents without explicit user approval.
- Added comprehensive unit tests for both authentication and webhook ingestion.

## Related
Phase 3 & Phase 4 Meta App Integration Plan

## Testing
- [x] Tested locally
- [x] No new errors/warnings in console
- [x] Existing functionality still works (manual check)
- [x] Backend Linter (ruff) passed
- [x] Backend Tests (pytest) passed

## What might break
Minimal risk as these are new endpoints and frontend components.

## Screenshots (if UI change)
N/A

## Checklist
- [x] No secrets/API keys committed
- [x] `.env.example` updated if new env vars were added
- [x] Code follows existing project structure/conventions
- [x] Self-reviewed the diff before requesting merge
