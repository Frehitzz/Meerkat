## Type
Feature / Fix

## Summary
Completes Phase 5 testing fixes and prepares the project for Render deployment. The Facebook Login flow was updated to correctly use Meta's new `config_id` required for Business Login and explicitly requests `pages_manage_metadata` to allow automated Webhook subscriptions. 

## Changes
- Updated `meerkat-backend/routes/auth.py` to use `config_id` instead of raw scopes.
- Added logic in `auth.py` to auto-subscribe the seller's Facebook Pages to the webhook upon successful login.
- Replaced hardcoded localhost URL in `meerkat-frontend/src/App.jsx` with `VITE_API_BASE_URL` environment variable.
- Added `.env.example` to the frontend directory.
- Updated `meerkat-backend/.env.example` to include `META_REDIRECT_URI`.

## Related
Phase 5 of Meta App Integration Plan

## Testing
- [x] Tested locally
- [x] No new errors/warnings in console
- [x] Existing functionality still works (manual check)
- [x] Webhook verified working against live Meta Graph API 

## What might break
Minimal risk. All endpoints updated safely.

## Screenshots (if UI change)
N/A

## Checklist
- [x] No secrets/API keys committed
- [x] `.env.example` updated if new env vars were added
- [x] Code follows existing project structure/conventions
- [x] Self-reviewed the diff before requesting merge
